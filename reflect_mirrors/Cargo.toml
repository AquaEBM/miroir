[package]
name = "reflect_mirrors"
version = "0.1.0"
edition = "2021"

[dependencies]

reflect = { path = "../reflect", default-features = false }
arrayvec = { version = "0.7", default-features = false }